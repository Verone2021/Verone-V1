#!/bin/sh
# PRE-PUSH MINIMAL — INFRA-DX-001 (2026-05-09)
#
# La CI GitHub Actions fait le type-check + lint complet sur la PR
# (workflow .github/workflows/quality.yml job "quality"). Refaire le même
# travail localement coûte 8-15s par push pour zéro information nouvelle.
#
# Le pre-push reste pour permettre l'ajout de hooks futurs (ex: scan
# secrets, blocage push direct sur main) sans recréer un hook vide.

exit 0
