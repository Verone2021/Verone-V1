echo "🔍 Validation pre-commit Vérone..."
echo ""

# 1. Type check
echo "📝 Type checking..."
npm run type-check || exit 1

# 2. Lint staged files
echo ""
echo "🔧 Linting staged files..."
npx lint-staged || exit 1

# 3. Check database type alignment (staged files only)
echo ""
echo "🗄️  Checking database type alignment..."
if [ -f ".claude/scripts/check-db-type-alignment.ts" ]; then
  node .claude/scripts/check-db-type-alignment.ts --staged || exit 1
else
  echo "⏭️  Skipping (script not found)"
fi

echo ""
echo "✅ Pre-commit validation réussie!"
echo ""
