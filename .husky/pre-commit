#!/bin/sh

# 1. Génération types désactivée (manuel via MCP recommandé)
# Raison : Types générés uniquement APRÈS apply_migration (MCP workflow)
# Pour régénérer manuellement : pnpm generate:types
# Ou via MCP : mcp__supabase__generate_typescript_types

# 2. Lint-staged (ESLint + Prettier)
npx lint-staged
