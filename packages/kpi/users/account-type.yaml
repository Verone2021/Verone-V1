# KPI: Type Compte

id: kpi-user-account-type
name: Type Compte (Utilisateur)
description: Type compte utilisateur (Équipe/Standard) basé sur rôle.

module: Profil et rôles
category: Indicateurs
owner: Romeo Dos Santos

formula: |
  type = role IN ['owner', 'admin', 'catalog_manager'] ? 'Équipe' : 'Standard'

source:
  table: user_profiles.role
  file: src/app/admin/users/[id]/page.tsx

displayed_in:
  - component: user-stats-cards.tsx
    location: "Card 'Type compte'"

output:
  type: enum
  values: ['Équipe', 'Standard']

metadata:
  created_at: 2025-10-22
  version: 2.0.0
  status: active
  priority: low

business_notes: |
  **Valeur Romeo** : Équipe (owner).
