import type { BrandTokens } from '../../types';

/**
 * Tokens de la marque Flos.
 * Label humain : "Flos" — slug technique : flos
 *
 * Statut : placeholder — à remplir dans BO-DS-002 après génération sur claude.ai web.
 * Référence : docs/architecture/HANDOFF-CLAUDE-AI.md
 */
export const flosTokens: BrandTokens | null = null;
