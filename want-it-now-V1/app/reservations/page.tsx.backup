import { Suspense } from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Plus, Calendar, Upload, Filter, Download } from 'lucide-react';
import Link from 'next/link';
import { ReservationsTable } from '@/components/reservations/reservations-table';
import { ReservationsFilters } from '@/components/reservations/reservations-filters';

export default function ReservationsPage() {
  return (
    <div className="container mx-auto p-6 space-y-6">
      {/* Header */}
      <div className="flex items-center justify-between">
        <div>
          <h1 className="text-3xl font-bold text-gray-900">Réservations</h1>
          <p className="text-gray-600 mt-1">
            Gestion des réservations multi-plateformes
          </p>
        </div>
        
        <div className="flex items-center space-x-3">
          <Button variant="outline" size="sm" asChild>
            <Link href="/reservations/calendar">
              <Calendar className="w-4 h-4 mr-2" />
              Calendrier
            </Link>
          </Button>
          
          <Button variant="outline" size="sm" asChild>
            <Link href="/reservations/import">
              <Upload className="w-4 h-4 mr-2" />
              Import CSV
            </Link>
          </Button>
          
          <Button size="sm" className="bg-[#D4841A] hover:bg-[#B8741A]" asChild>
            <Link href="/reservations/new">
              <Plus className="w-4 h-4 mr-2" />
              Nouvelle réservation
            </Link>
          </Button>
        </div>
      </div>

      {/* Stats Cards */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-6">
        <Card>
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium">Réservations actives</CardTitle>
            <Badge className="bg-green-100 text-green-800 border-green-200">
              Active
            </Badge>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold">24</div>
            <p className="text-xs text-gray-600">
              +2 depuis hier
            </p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium">Check-ins aujourd'hui</CardTitle>
            <Badge className="bg-[#D4841A]/10 text-[#D4841A] border-[#D4841A]/20">
              Aujourd'hui
            </Badge>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold">3</div>
            <p className="text-xs text-gray-600">
              2 propriétés, 1 unité
            </p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium">Check-outs demain</CardTitle>
            <Badge className="bg-blue-100 text-blue-800 border-blue-200">
              Demain
            </Badge>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold">5</div>
            <p className="text-xs text-gray-600">
              4 propriétés, 1 unité
            </p>
          </CardContent>
        </Card>

        <Card>
          <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
            <CardTitle className="text-sm font-medium">Revenus du mois</CardTitle>
            <Badge className="bg-[#2D5A27]/10 text-[#2D5A27] border-[#2D5A27]/20">
              Février
            </Badge>
          </CardHeader>
          <CardContent>
            <div className="text-2xl font-bold">12 450€</div>
            <p className="text-xs text-gray-600">
              +15% vs mois dernier
            </p>
          </CardContent>
        </Card>
      </div>

      {/* Filters and Table */}
      <Card>
        <CardHeader>
          <div className="flex items-center justify-between">
            <div>
              <CardTitle>Liste des réservations</CardTitle>
              <CardDescription>
                Gérez vos réservations de toutes les plateformes
              </CardDescription>
            </div>
            
            <div className="flex items-center space-x-2">
              <Button variant="outline" size="sm">
                <Filter className="w-4 h-4 mr-2" />
                Filtres
              </Button>
              <Button variant="outline" size="sm">
                <Download className="w-4 h-4 mr-2" />
                Export CSV
              </Button>
            </div>
          </div>
        </CardHeader>
        <CardContent>
          <Suspense fallback={<div>Chargement...</div>}>
            <ReservationsFilters />
            <ReservationsTable />
          </Suspense>
        </CardContent>
      </Card>
    </div>
  );
}