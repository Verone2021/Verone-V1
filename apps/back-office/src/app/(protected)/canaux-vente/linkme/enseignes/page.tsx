'use client';

import { EnseignesSection } from '../components/EnseignesSection';

/**
 * Page Enseignes LinkMe
 * Gestion des enseignes (réseaux de franchises/organisations)
 */
export default function LinkMeEnseignesPage() {
  return <EnseignesSection />;
}
