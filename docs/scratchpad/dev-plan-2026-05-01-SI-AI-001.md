# [SI-AI-001] Dev plan — Batch IA produits (213 produits)

**Date** : 2026-05-01
**Branche** : `feat/SI-AI-001-batch-ai-product-content`
**Worktree** : `/Users/romeodossantos/verone-si-ai-001`
**Cible** : 213 produits (active + preorder, archived_at IS NULL)

## Objectif

Enrichir 213 produits Vérone avec contenu marketing IA généré via Claude Code conversationnel (pas d'API externe, gratuit forfait Romeo). 8 champs par produit, mini-batches de 10, validation Romeo entre batches.

## Migration SQL

`supabase/migrations/20260501XXXXXX_si_ai_001_marketing_fields.sql`

```sql
ALTER TABLE products
  ADD COLUMN IF NOT EXISTS commercial_name varchar,
  ADD COLUMN IF NOT EXISTS description_short text,
  ADD COLUMN IF NOT EXISTS description_long text,
  ADD COLUMN IF NOT EXISTS tags text[] DEFAULT '{}'::text[],
  ADD COLUMN IF NOT EXISTS ai_generated_metadata jsonb DEFAULT '{}'::jsonb;

CREATE INDEX IF NOT EXISTS idx_products_tags ON products USING GIN (tags);
```

## Champs générés par produit

1. `commercial_name` (varchar) : nom Vérone, 3-6 mots, distinct du nom fournisseur
2. `description_short` (text) : 40-60 mots
3. `description_long` (text) : 200-400 mots, ton Architectural Digest
4. `meta_title` (text) : 50-60 char SEO
5. `meta_description` (text) : 140-160 char SEO
6. `tags` (text[]) : 5-10 tags
7. `slug` (varchar) : kebab-case depuis commercial_name, unicité testée
8. `alt_text` par image (product_images.alt_text) : 8-15 mots, vue Playwright

## Workflow par batch

1. Pull 10 produits (skip ceux avec `commercial_name IS NOT NULL` — idempotence)
2. Pour chaque produit : pull images via Playwright pour vue réelle (alt_text précis)
3. Génération 8 champs structurés
4. Présentation Romeo
5. Validation/édition
6. UPDATE atomique en transaction unique (products + product_images)
7. Enrichir `ai_generated_metadata` :
   ```json
   {
     "model": "claude-opus-4-7-1m",
     "prompt_version": "v1.0",
     "generated_at": "2026-05-01T...",
     "validated_at": "2026-05-01T...",
     "validated_by": "romeo",
     "batch_id": "batch-1"
   }
   ```

## Garde-fous

- Worktree isolé (PR #873 INFRA-IMG-006 tourne en parallèle, pas de conflit)
- Aucune modif fichier code de `@verone/products`
- Idempotence : `commercial_name IS NOT NULL` skip
- Rebase précoce avant chaque push
- Force-with-lease, jamais force nu

## Commits prévus

1. chore: scaffold task plan + migration SQL file
2. chore: regenerate Supabase types after marketing fields migration
3. feat: AI batch N (1-22) — N produits enrichis
4. (final) chore: regenerate auto-docs after batch complete

## Tests / vérifs

- Pas de fichier code modifié → pas besoin type-check sur app
- Type-check `@verone/types` après regen
- Vérifier RLS toujours active sur `products`
- Vérifier 0 trigger affecté (ALTER ADD COLUMN sans modification trigger)
