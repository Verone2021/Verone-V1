'use client';

/**
 * ⚠️ STUB TEMPORAIRE - Composant à implémenter
 *
 * Ce composant n'existait pas dans l'historique Git.
 * Il a été créé comme stub minimal pour débloquer le build.
 *
 * TODO: Implémenter l'upload d'images pour les collections
 * Utilisé dans: src/app/produits/collections/page.tsx (probablement)
 */

interface CollectionImageUploadProps {
  collectionId?: string;
  onUploadComplete?: (imageUrl: string) => void;
}

export function CollectionImageUpload(props: CollectionImageUploadProps) {
  // Stub: retourne null pour l'instant
  // L'utilisateur peut implémenter ce composant selon ses besoins
  return null;
}
