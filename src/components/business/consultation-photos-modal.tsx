'use client';

/**
 * ⚠️ STUB TEMPORAIRE - Composant à implémenter
 *
 * Ce composant n'existait pas dans l'historique Git.
 * Il a été créé comme stub minimal pour débloquer le build.
 *
 * TODO: Implémenter le modal photos pour consultations
 * Utilisé dans: src/shared/modules/consultations/components/images/ConsultationImageGallery.tsx
 */

interface ConsultationPhotosModalProps {
  open?: boolean;
  onClose?: () => void;
  consultationId?: string;
}

export function ConsultationPhotosModal(props: ConsultationPhotosModalProps) {
  // Stub: retourne null pour l'instant
  return null;
}
